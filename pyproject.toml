[project]
name = "kubernetes-explore"
version = "0.0.0"
description = "Tests for the kubernetes-explore Claude Code plugin scripts"
requires-python = "==3.13.*"

[dependency-groups]
dev = [
    "pytest>=8",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
